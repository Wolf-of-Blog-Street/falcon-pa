---
entity: me
description: <name> — <whose brain this is, one line>
date_created: <YYYY-MM-DD>
profile:
  important_to: []
---
Who this brain's subject is. "They are people; the brain's owner is *me*." The seed
registers the four budgeted lists (important_to/likes/dislikes/working_style) as real me.*
rows — keep them honest, prune past their budgets. A human-me fills them as an operator
model; an agent-me fills them as its own working identity and freely registers extra
fields (role/project/mission) — fields are free on every card, me included.

<!-- SEED TEMPLATE — the ME card: the ONE identity seed (brain-convergence). One card per
store, human or agent alike; the engine finds the subject by entity (no config key —
user_card is gone). Slug convention `me` (convention, not enforced). Schema on me is
NORMAL: brain register --entity me --field <x> --type <type> + brain set me --field
profile.<x> --to <value> works in any session, same as any card; only entity
TYPES are admin-mode. Whose brain this is is answered by what you write on it, not by a
mode. PRIVACY LINE (hard, when the subject is a person): the store stays in the consuming
project's private repo, never a shared/template repo. Fence is the first bytes so it
lints/imports cleanly. Install: copy to brain/__source/me.md, fill every <placeholder>,
then: brain lint … && brain sync --slug me. -->
