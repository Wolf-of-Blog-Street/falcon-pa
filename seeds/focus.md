---
entity: context
description: the bubble of happening — always active
date_created: <YYYY-MM-DD>
profile:
  name: Focus
  mission: the bubble of happening — always active
---

<!-- SEED TEMPLATE — the FOCUS context: the ONE reserved context, slug
`focus`, loaded in every session without asking (it cannot be dropped). Anything — any
entity, any tense — can be put in and out of focus freely: brain focus add <slug> --why
"…" / brain focus remove <slug> (sugar over in_context → focus membership edges; the
since-stamp folds into the edge why). Members rank FIRST in default search/list. Install:
copy to brain/__source/focus.md, fill <YYYY-MM-DD>, then: brain lint … && brain sync
--slug focus. Keep the slug `focus` — it is the reserved name the engine treats as
always-active. -->
